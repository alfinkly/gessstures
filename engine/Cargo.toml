[package]
name = "engine"
version.workspace = true
edition.workspace = true

[dependencies]
graph_core = { path = "../graph-core" }
physics_core = { path = "../physics-core" }
data_sources = { path = "../data-sources" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
petgraph = "0.7"
